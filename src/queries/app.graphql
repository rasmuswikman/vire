#import "./_categoryTree.graphql"

query App {
  storeConfig {
    product_url_suffix
    category_url_suffix
    default_title
    copyright
  }

  categoryList {
    id: uid
    uid
    children {
      ...categoryTree
    }
  }
}
